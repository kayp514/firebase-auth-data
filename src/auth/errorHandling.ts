// src/auth/errorHandling.ts

export function logAuthError(error: any): void {
    console.error('Authentication error:', error);
    // Implement more sophisticated logging here (e.g., to a logging service)
  }